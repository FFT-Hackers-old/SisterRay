#ifndef EVENT_H
#define EVENT_H

/*
 * FIXME: Should probably be removed since everything was moved
 * into the public headers.
 */

#include <SisterRay/SisterRay.h>
#include <unordered_set>

struct SrEventListener_ {
    u32 listenerID;
};

#endif
